{{-- The span needs to be there to enable vertical layouts (by setting display block on the span) --}}
<span class="ec-m-link__wrapper"><a href="{!! $href !!}" class="{{ $a_elm_css_classes }}" target="{{ $target }}">{{ $text }}</a></span>
